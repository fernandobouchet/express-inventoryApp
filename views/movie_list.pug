extends layout

block content
  h1= title

  ul
    each movie in movie_list
      li
        a(href=movie.url) #{movie.name}
        | (#{movie.category[0].name})
    else
      li There are no movies